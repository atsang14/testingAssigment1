package homework1;

import java.util.HashMap;

public class Error implements Processor {
    public String process(HashMap<String, String> hmap) {
//        String err = "This is awkward... you kinda didn't type in the correct url...";
        String err = "ERROR";
        return err;
    }
}
